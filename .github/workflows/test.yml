name: PyPI

# Run on PR requests. And on master itself.
on:
  push:
    branches:
      - master
  pull_request:

jobs:
  TestLinux:
    name: Python ${{ matrix.python }} ${{ matrix.display_name }} (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            python: "3.9"
            numpy: "==1.21.*"
            pins: "pygdal==3.4.1.* scipy==1.7.* dask[delayed]==2021.* pandas==1.3.* geopandas==0.* fiona==1.9.6"
          - os: ubuntu-22.04
            python: "3.10"
            numpy: "==1.23.*"
            pins: "pygdal==3.4.1.* scipy==1.9.* dask[delayed]==2021.7.* pandas==1.4.* geopandas==0.*"
          - os: ubuntu-22.04
            python: "3.11"
            numpy: "==1.*"
            pins: "pygdal==3.4.1.* scipy==1.11.* dask[delayed]==2023.* pandas==2.0.* geopandas==0.*"
          - os: ubuntu-22.04
            python: "3.12"
            numpy: "==1.*"
            pins: "pygdal==3.4.1.* scipy==1.13.*"
          # Uncomment when numpy 2.* is supported
          # - os: ubuntu-24.04
          #   python: "3.13"
          #   numpy: "==2.1.*"
          #   pins: "pygdal==3.4.1.* scipy==1.16.*"
          # - os: ubuntu-24.04
          #   python: "3.14"
          #   numpy: "==2.*"
          #   pins: "pygdal==3.4.1.*"

    steps:
      - uses: actions/checkout@v2

      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python }}

      - name: Install GDAL
        run: |
          sudo apt-get update && sudo apt-get install --yes --no-install-recommends libgdal-dev

      - name: Install python dependencies
        shell: bash
        # NumPy is installed first because the pygdal build needs it.
        run: |
          pip install --disable-pip-version-check --upgrade pip setuptools wheel uv
          uv pip install --system numpy${{ matrix.numpy }}
          uv pip install --system -e .[test,cityhash] ${{ matrix.pins }}
          uv pip list

      - name: Run tests
        shell: bash
        run: |
          uv run pytest dask_geomodeling

  TestDockerFocal:
    name: Docker ubuntu:focal tests
    runs-on: ubuntu-latest
    container:
      image: ubuntu:focal
    env:
      DEBIAN_FRONTEND: noninteractive

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          apt-get update && apt-get install --yes --no-install-recommends \
            build-essential \
            python3-dev \
            python3-pip \
            libgdal-dev

      - name: Install python dependencies
        shell: bash
        run: |
          pip3 install --disable-pip-version-check --upgrade pip setuptools wheel uv
          uv pip install --system -e .[test,cityhash] numpy==1.24.4 pygdal==3.0.4.11 scipy==1.10.1 dask[delayed]==2023.5.0 pandas==2.0.3 geopandas==0.13.2 fiona==1.9.6
          uv pip list

      - name: Run tests
        shell: bash
        run: |
          uv run pytest dask_geomodeling
